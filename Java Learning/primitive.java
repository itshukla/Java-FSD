public class primitive {
    public static void main(String[] args) {
        byte by = 123;
        short sh = 345;
        int in = 456;
        long lo = 789;

        System.err.println("BYTE DataType," + by+ "  SHORT DataType," +sh+ "   INTEGER DataType," + in+ "  LONG DataType," + lo);

        float fl = 1.23f;
        double db = 1.23;

        System.err.println("FLOAT DataType," + fl+ "  DOUBLE DataType," + db);

        char ch = 'a';
        boolean boo = true;
        System.err.println("CHAR DataType," + ch+ "  BOOLEAN DataType," + boo);
    
        
    }
}
